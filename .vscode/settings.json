{
    "containers.commands.composeUp": [
        {
            "label": "Compose Up",
            "template": "${composeCommand} ${configurationFile} up --watch ${detached} ${build}"
        }
    ],
    "containers.composeDetached": false
}